role_files: "{{ role_path }}/files"
role_templates: "{{ role_path }}/templates"

role_resource:
  touchapd_files: "{{ role_files }}/touchpad"
  touchpad_templates: "{{ role_templates }}/touchpad"

dotdir:
  touchpad: "~/.config/autostart"
