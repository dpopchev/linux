configs_home: "{{ role_path }}/files"

main_dotfile:
  src: gitconfig
  dest: ~/.gitconfig

attributes:
  src: gitattributes
  dest: ~/.gitattributes

aliases:
  - bash:
    src: aliases.sh
    dest: ~/.config/bashrc.d/git.aliases
